# Unreal Engine generated / build artifacts — never commit these.
Binaries/
Intermediate/
Saved/
DerivedDataCache/
.vs/
*.VC.db
*.opensdf
*.sdf
*.sln
*.suo
*.xcodeproj/
*.xcworkspace/

# Packaged builds (the playable .exe output) — distribute via GitHub Releases, never commit.
Releases/
*.zip

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Keep: Source/, Content/, Config/, Tools/, *.uproject, README.md
